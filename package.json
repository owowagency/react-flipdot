{
  "name": "react-flipdot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "pnpm turbo clean",
    "build": "pnpm turbo build",
    "build:watch": "pnpm run --parallel build:watch",
    "build:types:watch": "pnpm run --parallel build:types:watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.4.1",
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
