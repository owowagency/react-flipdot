import FlipdotDOM from "@owowagency/flipdot-dom";

/**
 * This method should mutate the container root node and remove all children from it.
 */
export function clearContainer(container: FlipdotDOM) {
    container.clear();
}