/**
 * This method is called just before the commit phase. 
 * Use it to set up any necessary state while any Host Components that might suspend this commit are evaluated to determine if the commit must be suspended.
 */
export function startSuspendingCommit() {

}